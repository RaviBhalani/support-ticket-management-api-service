# Required for local development.
# Docker Compose --env-file accepts only a single file for variable substitution in the
# compose YAML (e.g. ${SERVER_PORT}, ${POSTGRES_PORT}). Since api.env and db.env are
# separate, this file merges the variables needed for substitution in one place.
SERVER_ENVIRONMENT=local
SERVER_PORT=8000
POSTGRES_PORT=5432